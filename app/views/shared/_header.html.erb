<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <li><%= link_to("Search", search_path) %></li>
        <% if user_signed_in? %>
        <li><%= link_to("Me", profile_path) %></li>
        <% end %>
        <% if user_area.present? %>
        <li>
          <%= link_to(user_area.name, "#", class: 'tight-right') %>
          <%= link_to('(change?)', edit_area_path(set_area_redirect: request.original_url), class: 'tight-left') %>
        </li>
        <li><%= link_to("#{user_area.zip_codes.first.county} County", "#") %></li>
        <li><%= link_to(user_area.zip_codes.first.state, "#") %></li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li><%= link_to("Sign Out", destroy_user_session_url, method: :delete) %></li>
        <% else %>
          <li><%= link_to("Sign In", new_user_session_url) %></li>
          <li><%= link_to("Sign Up", new_user_registration_url) %></li>
        <% end %>
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</nav>