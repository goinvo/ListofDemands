<div class="row">
  <div class="col-xs-10">
    <%= form_tag search_path, id: "demand-search", method: "GET", class: "form-inline" do %>
      <div class="form-group">
        <label class="sr-only" for="q">Enter search</label>
        <%= text_field_tag("q", params[:q], class: "form-control", placeholder: "search", data: {"turbolinks-permanent" => true}) %>
      </div>
    <% end %>
  </div>
  <div class="col-xs-2">
    <%= link_to("+", new_demands_path, class: "btn btn-primary") %>
  </div>
</div>

<%= render(partial: 'demand', collection: @demands) %>